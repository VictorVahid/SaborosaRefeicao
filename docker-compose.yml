version: '3.8'

services:
  app:
    container_name: saborosa-refeicao
    image: saborosa-app:latest  # <--- ADICIONE ESTA LINHA AQUI
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: always